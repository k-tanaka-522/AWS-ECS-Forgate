# AWS インフラ実装定義（業務管理システム）

## 1. コード化するリソース

### **VPCとネットワーク**
- VPC（/16 CIDR）
- パブリックサブネット（ALB用）
- プライベートサブネット（ECS, RDS用）
- セキュリティグループ
  - ALB用
  - ECS用
  - RDS用

### **コンピューティング（ECS）**
- ECSクラスター
- タスク定義
  - コンテナイメージ
  - CPU/メモリ設定
  - ポート設定
- ECSサービス
  - オートスケーリング設定
  - ヘルスチェック設定

### **データベース（RDS）**
- RDSインスタンス
  - インスタンスタイプ
  - マルチAZ設定
  - バックアップ設定
  - パラメータグループ

### **認証（Cognito）**
- ユーザープール
- アプリクライアント
- IDプール（必要な場合）

### **ロードバランサー（ALB）**
- ALB
- ターゲットグループ
- リスナー設定

## 2. 手動設定が必要なリソース

### **ドメインとSSL証明書**
- ドメイン取得（Route 53または他のレジストラ）
- ACM証明書のドメイン検証
- Route 53のDNSレコード設定

### **ALBのSSL設定方針**
- **目的**: ALB（Application Load Balancer）を使用して、HTTPSを通じて安全にトラフィックを処理する
- **手順**:
  1. **ACMでの証明書取得**:
     - `dev.tk-niigata.com`のSSL証明書をAWS Certificate Manager（ACM）で取得
     - DNS検証を使用して証明書を検証
  2. **ALBのリスナー設定**:
     - ALBのリスナーにHTTPS設定を追加し、ACMで取得した証明書を使用
     - CloudFormationまたはTerraformを使用して、リスナー設定をコード化
  3. **セキュリティの強化**:
     - SSLポリシーを設定し、最新のセキュリティ基準に準拠

- **注意事項**:
  - 証明書のARNは、CloudFormationまたはTerraformのテンプレートで動的に取得
  - 証明書の自動更新をACMで設定し、手動での更新作業を省略

### **初期データとアカウント**
- 管理者アカウントの作成
- 初期データのセットアップ
- テストユーザーの作成

### **監視とアラート**
- CloudWatch アラーム
- SNSトピック
- メール通知設定

**注意事項**:
- サンプル段階では監視とアラートの設定をコード化する必要はありません
- 実際の構築が始まった際には、以下の項目をコード化することを推奨します：
  - CloudWatchアラームの設定
  - SNSトピックの作成と設定
  - メール通知の設定
- コード化により、全環境での一貫性を保ち、管理を容易にします

## 3. 環境構築手順

### **事前準備**
1. AWSアカウントの準備
2. 開発環境の構築
   - AWS CLI
   - Terraform/CloudFormation
   - Docker

### **インフラのデプロイ手順**
1. VPCとネットワークの構築
2. RDSの構築
3. ECSクラスターの構築
4. Cognitoの設定
5. ALBの構築
6. アプリケーションのデプロイ

### **テスト手順**
1. ネットワーク接続性の確認
2. RDS接続の確認
3. アプリケーションの動作確認
4. Cognito認証の確認

## 4. 構成管理コード

### **CloudFormation**
- テンプレートの構成
  - network.yaml（VPC、サブネット、セキュリティグループ）
  - database.yaml（RDS）
  - compute.yaml（ECS、ALB）
  - auth.yaml（Cognito）

### **Terraform**
- モジュール構成
  - network/（VPC、サブネット、セキュリティグループ）
  - database/（RDS）
  - compute/（ECS、ALB）
  - auth/（Cognito）

## 5. 環境変数と設定

### **必要な環境変数**
```
# アプリケーション設定
APP_ENV=development
APP_PORT=3000

# データベース設定
DB_HOST=<RDSエンドポイント>
DB_PORT=3306
DB_NAME=escforgate
DB_USER=admin

# Cognito設定
COGNITO_USER_POOL_ID=<ユーザープールID>
COGNITO_CLIENT_ID=<アプリクライアントID>
```

### **シークレット管理**
- AWS Secrets Managerで管理する項目
  - データベースパスワード
  - アプリケーションシークレット
  - APIキー

## 6. セキュリティ設定

### **IAMロールとポリシー**
- ECSタスク実行ロール
- ECSタスクロール
- RDSアクセスロール

### **セキュリティグループルール**
- ALB -> ECS（アプリケーションポート）
- ECS -> RDS（3306）
- 管理用アクセス

## 7. バックアップと復旧

### **バックアップ対象**
- RDSスナップショット
- ECSタスク定義
- アプリケーションデータ

### **復旧手順**
1. RDSスナップショットからの復元
2. アプリケーションデータの復元
3. ECSサービスの再デプロイ

## 8. サンプル構築の方針

### **目的**
- サンプル構築では、最小限のリソースを使用して基本的な動作を確認します
- 監視やアラート等の運用機能は含めず、基本機能の動作検証に焦点を当てます

### **使用するリソース**
- **ECS**: 
  - コンテナ化されたアプリケーションのデプロイと管理
  - 基本的なオートスケーリング設定

- **RDS**: 
  - データベースの管理とデータの永続化
  - マルチAZ構成での基本的な冗長性確保

- **Cognito**: 
  - ユーザー認証と管理
  - 基本的な認証フローの確認

### **構築の範囲**
- 上記の最小限のリソースを使用して、基本的なWebサービスが動作することを確認
- 各コンポーネント間の連携が正常に機能することを検証
- 本番環境で必要となる高度な設定や運用機能は、サンプル構築後に段階的に追加
