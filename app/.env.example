# Node Environment
NODE_ENV=production

# Service Name (set per service)
SERVICE_NAME=sample-app-public

# Logging
LOG_LEVEL=info

# Server Configuration
PORT=3000

# Database Configuration
DB_HOST=your-rds-endpoint.rds.amazonaws.com
DB_PORT=5432
DB_NAME=sampleappdb
DB_USER=postgres
DB_PASSWORD=your-secure-password

# CORS Configuration (for public service)
CORS_ORIGIN=*
