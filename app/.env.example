# ==============================================================================
# Environment Variables Example
# ==============================================================================
# このファイルをコピーして .env を作成し、実際の値を設定してください
# cp .env.example .env
# ==============================================================================

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=myapp_dev
DB_USER=postgres
DB_PASSWORD=

# Application Configuration
NODE_ENV=development
PORT=8080

# Batch Processing Configuration (batch service only)
BATCH_INTERVAL=3600000  # 1 hour in milliseconds (1時間ごとに実行)

# Optional: Database SSL
# DB_SSL=false
